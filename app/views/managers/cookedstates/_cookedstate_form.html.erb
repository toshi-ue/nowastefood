<% if cookedstate.errors.any? %>
  <div id="error_explanation">
<!--      <h2><%= pluralize(cookedstate.errors.count, "error") %> prohibited this aaa from being saved:</h2>
-->
    <ul>
    <% cookedstate.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, id: :cookedstate_name, autofocus: true, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :foodcategory_id %>
    <%= form.collection_select(:foodcategory_id, Foodcategory.all, :id, :name, {}, {class: 'form-control'}) %>
  </div>
