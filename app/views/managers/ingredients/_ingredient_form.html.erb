<%= render 'shared/error_messages', {resource: ingredient} if ingredient.errors.any? %>

<div class="form-group">
  <%= form.label :name %>
  <%= form.text_field :name, id: :ingredient_name, class: 'form-control' %>
</div>
