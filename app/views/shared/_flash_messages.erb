<div class="mt-3">
  <% flash.each do |key, value| %>
    <% next if key == "timedout" %>
    <div class="alert alert-<%= bootstrap_alert(key) %>">
      <strong>
        <%= value %>
      </strong>
    </div>
  <% end %>
</div>
